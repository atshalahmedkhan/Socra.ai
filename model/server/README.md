# Socra vLLM
Pinned to vLLM 0.9.2. Verify `vllm serve --help` whenever updating the pin. Accept Gemma terms and supply secrets at runtime. Missing enabled adapters stop startup. Ports 8001/8002 are private by default.
