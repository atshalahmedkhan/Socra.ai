# Socra backend runtime dependencies.
# Core stack needed for the app to boot and serve health/status.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<1.0

# --- Enable as features land (kept out of the boot path for a lean scaffold) ---
# Database + pgvector
# sqlalchemy[asyncio]>=2.0,<3.0
# asyncpg>=0.29
# pgvector>=0.3
# Supabase
# supabase>=2.6
# Auth (JWT verification)
# pyjwt>=2.9
# PDF extraction
# pymupdf>=1.24
# Embeddings (local BGE/E5)
# sentence-transformers>=3.0
# Uploads
# python-multipart>=0.0.9
